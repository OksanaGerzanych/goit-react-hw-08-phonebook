import { Nav } from './Navigation.styled';

export const Navigation = () => {
    return (
        <nav>
        <Nav to="/">Home</Nav>
        <Nav to="/contacts">Contacts</Nav>  
       </nav> 
    )
}